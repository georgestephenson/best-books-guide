# Content drafts (kept local, not versioned)
/ideas

# Dependencies
node_modules/

# Build output
dist/
*.tsbuildinfo

# Test / coverage
coverage/

# Environment
.env
.env.*
!.env.example

# Editor / OS
.DS_Store
*.log

# Terraform (added in the infra phase)
**/.terraform/*
*.tfstate
*.tfstate.*
!*.tfvars.example
*.tfvars

# Ansible (added in the infra phase)
infra/ansible/**/vault_pass.txt
# Encrypted vault stays local — public repo, so we never publish secret ciphertext.
# deploy.yml (the CI-run playbook) doesn't need it; recreate from vault.yml.example.
infra/ansible/group_vars/all/vault.yml
