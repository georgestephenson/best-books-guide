---
all:
  hosts:
    bestbooks-prod:
      # Elastic IP from: terraform -chdir=infra/terraform/envs/prod output instance_public_ip
      ansible_host: 203.0.113.4 # CHANGEME
  vars:
    ansible_user: ubuntu
    ansible_python_interpreter: /usr/bin/python3
