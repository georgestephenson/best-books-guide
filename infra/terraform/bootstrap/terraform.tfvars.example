# Copy to terraform.tfvars (gitignored) and fill in.
# Only budget_notify_email is required; the rest default sensibly (see variables.tf).

budget_notify_email = "you@example.com"

# aws_region  = "eu-west-2"
# github_repo = "georgestephenson/best-books-guide"
